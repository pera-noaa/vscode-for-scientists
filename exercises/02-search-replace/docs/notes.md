# Project notes

## Background
The `phoenix-prototype` was the original codename for the calibration pipeline
spun up in 2024. Several modules and scripts still reference it by that name.

## Pending rename
Per the 2026-Q1 planning, `phoenix-prototype` will be renamed to `aurora-v2`.
The rename touches:

- `config.yaml`
- Several scripts under `analysis/`
- This document
- Internal documentation (not in this repo)

After the rename, only the historical context in this file (and the migration
notes) will retain the old name.
